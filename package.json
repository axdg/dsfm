{
  "name": "dsfm",
  "version": "0.1.1",
  "repository": "https://github.com/axdg/dark-matter.git",
  "description": "Delimiter Seperated Front-Matter",
  "main": "index.js",
  "scripts": {
    "lint": "eslint index.js test.js",
    "test": "./node_modules/eslint/bin/eslint.js *.js && mocha --reporter spec",
    "test:coverage": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "author": "axdg <axdg@dfant.asia>",
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^4.1.6",
    "coveralls": "^2.11.6",
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^3.1.0",
    "istanbul": "^0.4.2",
    "mocha-lcov-reporter": "^1.0.0",
    "tape": "^4.4.0"
  },
  "dependencies": {
    "js-yaml": "^3.5.2",
    "readable-stream": "^2.0.5"
  }
}
