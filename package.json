{
  "name": "dsfm",
  "version": "0.0.2",
  "repository": "https://github.com/axdg/dark-matter.git",
  "description": "Delimiter Seperated Front-Matter",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec && istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "author": "axdg <axdg@dfant.asia>",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.11.6",
    "istanbul": "^0.4.2",
    "mocha-lcov-reporter": "^1.0.0"
  },
  "dependencies": {
    "js-yaml": "^3.5.2",
    "readable-stream": "^2.0.5"
  },
  "config": {
    "blanket": {
      "pattern": "index.js",
      "data-cover-never": [
        "node_modules",
        "test.js"
      ]
    }
  }
}
